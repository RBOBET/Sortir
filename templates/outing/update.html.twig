{% extends 'base.html.twig' %}


{% block title %} {{ parent() }} | modifier sortie {% endblock %}

{% block pageTitle %}
    <h2>Modifier votre sortie</h2>
{% endblock %}

{% block body %}


    {{ form_start(outingForm) }}


    {{ form_widget(outingForm) }}
    {# <div>
    <button>Enregistrer</button>
    <button>Publier la sortie</button>
    #}

    {{ form_end(outingForm) }}
    <a class="btn" href="{{ path('place_add') }}">+</a>
    <a class="btn" href="{{ path('outing_show', {'id': outing.id }) }}">Annuler</a>
    {#   </div>     #}

{% endblock %}

